import os
import geolocation
from geolocation import states, postal codes, 

# Importing the pandas package.
import pandas as pd
def processdata():
    # Loading and reading the CSV file.
    df = pd.read_csv('collision_accident.csv', delimiter = ',')
    # Converting the CSV data to a list.
    df = df.to_dict(orient = 'records')
    return df
  
  state= {florida} return key = 1 'state'
  
  
